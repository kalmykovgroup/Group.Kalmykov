FROM nginx
  

ARG SSL_FILE_KEY
ARG SSL_FILE_CHAIN
ARG NGINX_DOMAIN_NAMES


ENV SSL_FILE_CHAIN=$SSL_FILE_CHAIN
ENV SSL_FILE_KEY=$SSL_FILE_KEY
ENV NGINX_DOMAIN_NAMES=$NGINX_DOMAIN_NAMES


CMD ["nginx", "-g", "daemon off;"]